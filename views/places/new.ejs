<!DOCTYPE html>
<html>
    <head>
        <title>Unovoyage</title>
        <style type="text/css">
            h1{
                text-decoration-color: #000000;
                font-weight: bold;
                color: #000000;

            }
            body {
                background: url("../images/airplane.jpg") no-repeat center center fixed;
                background-size: cover;
                 font-size: 16px;
                    font-family: 'Lato', sans-serif;
                font-weight: 300;
                align-items: center;
                align-content: center;
                         margin: 0;
                color: #666;
}
    input {
background-color: rgba(0,0,0,0.1);
width=80%;
padding: 5px;
margin: auto;
width: 350px;
text-decoration-color: black;
font-weight: bold;
color: #000000;
}
::-webkit-input-placeholder { /* Chrome */
  color: black;
  font-weight: bold;
}
:-ms-input-placeholder { /* IE 10+ */
  color: black;
  font-weight: bold;
}
::-moz-placeholder { /* Firefox 19+ */
  color: black;
 opacity: 1;
 font-weight: bold;
}
:-moz-placeholder { /* Firefox 4 - 18 */
  color: black;
  opacity: 1;
  font-weight: bold;
}
        </style>
        <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" rel="stylesheet">

    </head>
    <body>
 <nav class="navbar navbar-inverse">
        <div class="container">
            <div class="navbar-header">
               <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-nav-demo" aria-expanded="false">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                 <% if(!currentUser) {%>
                    <a class="navbar-brand" href="/">Unovoyage</a> <!--For not logged in send to Home Page -->
                <% } else {%>
                     <% if(currentUser.isAdmin) {%>
                    <a class="navbar-brand" href="/places">Unovoyage</a>
                    <% }else if(currentUser.isManager) { %>
                    <a class="navbar-brand" href="/places/manager">Unovoyage</a><!--If logged in send to Dashboard  -->
                    <% } else {%>
                    <a class="navbar-brand" href="/places/user">Unovoyage</a> <!--If logged in send to Dashboard  -->
                    <% } %>
               <% } %>

            </div>
            <div class="collapse navbar-collapse" id="bs-nav-demo">
                <ul class="nav navbar-nav navbar-right">
                 <% if(!currentUser) {%>
                    <li><a href="/adminLogin">Log In   <i class="fa fa-user"></i></a></li>
                    <li><a href="/register">Sign Up   <i class="fa fa-user-plus"></i></a></li>
                <% } else {%>
                    <% if(currentUser.isAdmin) {%>
                    <li><a href="/places"><%= currentUser.username %> <i class="fa fa-user"></i> </a></li>
                    <% }else if(currentUser.isManager) { %>
                    <li><a href="/places/manager"><%= currentUser.username %> <i class="fa fa-user"></i> </a></li>                    
                    <% } else {%>
                    <li><a href="/edit"><%= currentUser.username %> <i class="fa fa-user"></i> </a></li>
                <% } %>
                    <li><a href="/logout"> Log out  <i class="fa fa-sign-out" aria-hidden="true"></i>
                </a></li>
                <% } %>
                </ul>
            </div>
        </div>
    </nav>

<div class="container">
    <div class="row">
         <h1 style="color:black;" class="text-center">Add a New Place</h1>
         <!--Styled to center the form-->
         <div style="width: 30%; margin: 3% auto;">
             <form method="POST" action="/places">
                 <!-- For spacing b/w the form elements we use class="form-group" -->
                 <div class="form-group">
                     <input type="text" name="name" placeholder="Place name" required >
                 </div>
                 <div class="form-group">
                      <input type="text" name="image" placeholder="image url"  required>
                 </div>
                 <div class="form-group">
                      <input type="text" name="description" placeholder="description" required>
                 </div>
                 <div class="form-group">
                     <button class="btn btn-lg btn-success btn-block" style="opacity: 0.8">Submit</button>
                 </div>
             </form>
             <p>
                 <!--class="btn-block" just takes the size of the parent element-->
                 <a class="btn btn-lg btn-danger btn-block " style="opacity: 0.8" href="/places">Go Back</a>
             </p>
         </div>
    </div>
</div>
 
<% include ../partials/footer %>