<!DOCTYPE html>
<html>
    <head>
        <title>Unovoyage</title>
        <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" rel="stylesheet">
        <link href="/stylesheets/main.css" rel="stylesheet">
        <link rel="stylesheet" type="text/css" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.4.0/css/font-awesome.min.css">
  <link href='https://fonts.googleapis.com/css?family=Roboto+Slab:400,100,300,700|Lato:400,100,300,700,900' rel='stylesheet' type='text/css'>
  <link rel="stylesheet" href="/stylesheets/animate.css">
  <link rel="stylesheet" href="/stylesheets/style2.css">

        
    </head>
    <body>
    <style type="text/css">
        li{
            font-weight: bold;
        }
       body {
                background: url("../images/agra.jpg") no-repeat center center fixed;
                background-size: cover;
                 font-size: 16px;
                    font-family: 'Lato', sans-serif;
                font-weight: 300;
                align-items: center;
                align-content: center;
                         margin: 0;
                color: #666;
        }
    </style>

    <nav class="navbar navbar-inverse">
        <div class="container">
            <div class="navbar-header">
               <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-nav-demo" aria-expanded="false">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                 <% if(!currentUser) {%>
                    <a class="navbar-brand" href="/">Unovoyage</a> <!--For not logged in send to Home Page -->
                <% } else {%>
                     <% if(currentUser.isAdmin) {%>
                    <a class="navbar-brand" href="/places">Unovoyage</a>
                    <% }else if(currentUser.isManager) { %>
                    <a class="navbar-brand" href="/places/manager">Unovoyage</a><!--If logged in send to Dashboard  -->
                    <% } else {%>
                    <a class="navbar-brand" href="/places/user">Unovoyage</a> <!--If logged in send to Dashboard  -->
                    <% } %>
               <% } %>

            </div>
            <div class="collapse navbar-collapse" id="bs-nav-demo">
                <ul class="nav navbar-nav navbar-right">
                 <% if(!currentUser) {%>
                    <li><a href="/adminLogin">Log In   <i class="fa fa-user"></i></a></li>
                    <li><a href="/register">Sign Up   <i class="fa fa-user-plus"></i></a></li>
                <% } else {%>
                    <% if(currentUser.isAdmin) {%>
                    <li><a href="/places"><%= currentUser.username %> <i class="fa fa-user"></i> </a></li>
                    <% }else if(currentUser.isManager) { %>
                    <li><a href="/places/manager"><%= currentUser.username %> <i class="fa fa-user"></i> </a></li>                    
                    <% } else {%>
                    <li><a href="/edit"><%= currentUser.username %> <i class="fa fa-user"></i> </a></li>
                <% } %>
                    <li><a href="/logout"> Log out  <i class="fa fa-sign-out" aria-hidden="true"></i>
                </a></li>
                <% } %>
                </ul>
            </div>
        </div>
    </nav>


        <div class="container">
          <div class="top">
              <form method="POST" action="/edit?_method=PUT">
              <div class="login-box animated fadeInUp">
                  <%if(updationstatus == 1 ){%>
                       <p style="color: red; font-weight: bold; padding-bottom: 1px;">Enter valid Email id</p>
                  <%} else if(updationstatus==2){%>
                       <p style="color: red; font-weight: bold; padding-bottom: 1px;">Enter valid contact no.</p>
                  <%}%>
                    <div class="box-header">
                          <h2 class="text-muted">Update Details</h2>
                    </div>
     
      <label for="email" >Email</label>
      <br/>
      <input type="email" id="email" name="email" value="<%=currentUser.email%>" required>
      <br/>
      <label for="name" >Name</label>
      <br/>
      <input type="text" id="password" name="name" value="<%=currentUser.name%>" required>
      <br/>
      <label for="contact" >Contact</label>
      <br/>
      <input type="contact" id="contact" name="contact" value="<%=currentUser.contact%>" required>
      <br/>
       <br/>
     
      <button type="submit">Update</button>
      <br/>
    </div>
  </form>
</div>
</div>

<% include ./partials/footer.ejs%>